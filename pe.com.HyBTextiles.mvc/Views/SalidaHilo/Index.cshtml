@model IEnumerable<pe.com.HyBTextiles.mvc.Models.SalidaHilo>

@{
    ViewBag.Title = "Listado de SalidaHilo";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-4">
    <div class="card shadow border-0">
        <div class="card-header bg-dark text-white d-flex justify-content-between align-items-center">
            <h4 class="mb-0">Listado de SalidaHilo</h4>
            @Html.ActionLink("Registrar", "Create", null, new { @class = "btn btn-primary" })
        </div>
        <div class="card-body">
            <div class="mb-3"><span class="badge bg-secondary">Total de Registros: @Model.Count()</span></div>
            <div class="table-responsive">
                <table class="table table-striped table-bordered">
                    <thead><tr>
                        <th>Código</th>
                        <th>Fecha Salida</th>
                        <th>Producción</th>
                        <th>Tipo Hilo</th>
                        <th>Unidad Medida</th>
                        <th>Cantidad Salida</th>
                        <th>Estado</th>
                        <th>Acciones</th>
                    </tr></thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@Html.DisplayFor(modelItem => item.codsal)</td>
                                <td>@Html.DisplayFor(modelItem => item.fecsal)</td>
                                <td>@Html.DisplayFor(modelItem => item.codpro)</td>
                                <td>@Html.DisplayFor(modelItem => item.codthi)</td>
                                <td>@Html.DisplayFor(modelItem => item.codund)</td>
                                <td>@Html.DisplayFor(modelItem => item.cansal)</td>
                                <td>@(item.estsal ? "Habilitado" : "Deshabilitado")</td>
                                <td class="text-center" style="white-space:nowrap">
                                    @Html.ActionLink("Actualizar", "Edit", new { id = item.codsal }, new { @class = "btn btn-success btn-sm" })
                                    @Html.ActionLink("Eliminar", "Delete", new { id = item.codsal }, new { @class = "btn btn-danger btn-sm" })
                                    @Html.ActionLink("Habilitar", "Enable", new { id = item.codsal }, new { @class = "btn btn-warning btn-sm" })
                                    @Html.ActionLink("Ver Detalle", "Details", new { id = item.codsal }, new { @class = "btn btn-dark btn-sm" })
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
        <div class="card-footer text-muted text-center">HyB Textiles - SalidaHilo</div>
    </div>
</div>