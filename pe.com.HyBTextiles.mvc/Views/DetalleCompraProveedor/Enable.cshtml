@model pe.com.HyBTextiles.mvc.Models.DetalleCompraProveedor

@{
    ViewBag.Title = "Habilitación de DetalleCompraProveedor";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-4"><div class="card shadow border-0">
    <div class="card-header bg-warning text-white"><h4 class="mb-0">Habilitación de DetalleCompraProveedor</h4></div>
    <div class="card-body">
        <div class="alert alert-warning">¿Estás seguro que deseas habilitar este registro?</div>
        <dl class="row">
            <dt class="col-sm-3">Código</dt>
            <dd class="col-sm-9">@Html.DisplayFor(model => model.coddco)</dd>
            <dt class="col-sm-3">Compra</dt>
            <dd class="col-sm-9">@Html.DisplayFor(model => model.codcom)</dd>
            <dt class="col-sm-3">Tipo Hilo</dt>
            <dd class="col-sm-9">@Html.DisplayFor(model => model.codthi)</dd>
            <dt class="col-sm-3">Unidad</dt>
            <dd class="col-sm-9">@Html.DisplayFor(model => model.codund)</dd>
            <dt class="col-sm-3">Cantidad</dt>
            <dd class="col-sm-9">@Html.DisplayFor(model => model.candco)</dd>
            <dt class="col-sm-3">Precio</dt>
            <dd class="col-sm-9">@Html.DisplayFor(model => model.preciodco)</dd>
            <dt class="col-sm-3">Estado</dt>
            <dd class="col-sm-9">@(Model.estdco ? "Habilitado" : "Deshabilitado")</dd>
        </dl>
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            <div class="d-flex justify-content-between">
                <button type="submit" class="btn btn-warning">Habilitar</button>
                @Html.ActionLink("Regresar", "Index", null, new { @class = "btn btn-dark" })
            </div>
        }
    </div>
    <div class="card-footer text-muted text-center">HyB Textiles - DetalleCompraProveedor</div>
</div></div>